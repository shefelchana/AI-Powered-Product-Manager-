# Leave DATABASE_URL unset locally: the backend falls back to a SQLite file
# (backend/data.sqlite), so there is no database to install.
# On Render the Blueprint injects this automatically and the backend uses Postgres.
# DATABASE_URL=postgresql://user:password@host:5432/app

PORT=3001
NODE_ENV=development
